# Caching compiled regexs for better performance
reDigit = /[0-9]/
reDigits = /^[0-9]+/
reLetter = /[a-zA-Z]/
reLetters = /^[a-zA-Z]+/
reWhitespaces = /^\s+/
reErrorExpectation = /ParseError.+Expecting/

module.exports = {
  reDigit
  reDigits
  reLetter
  reLetters
  reWhitespaces
  reErrorExpectation
}